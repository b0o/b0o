name: keepwarm
on:
  workflow_dispatch: {}
  schedule:
    - cron: "*/5 * * * *"
jobs:
  keepwarm:
    runs-on: ubuntu-latest
    steps:
      - name: Poll the Vercel serverless function to keep it warm
        run: curl --verbose https://github-readme-stats-eight-topaz-65.vercel.app/
